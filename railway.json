{
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "python api_server.py & python main.py",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ALWAYS"
  }
}
