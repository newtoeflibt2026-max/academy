{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn run_webapp:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
