# 📌 INSTRUCTIONS — إرشادات دائمة لمساعد التطوير

## قواعد التنفيذ (لا تُكسر)
1. **لا فتح ملفات يدوياً** — جميع الأوامر PowerShell جاهزة للّصق، تُنشئ/تُعدّل الملفات تلقائياً.
2. **نسخة احتياطية قبل أي تعديل** على ملف موجود: `<file>.bak_<reason>_<yyyyMMdd_HHmmss>`.
3. **فحص Syntax** بعد كل تعديل Python (`py -m py_compile`) أو HTML.
4. **لا `git push` قبل تأكيد صريح** من المستخدم.
5. **مراجعة هذا الملف قبل كل إجراء.**

## قواعد المحتوى التعليمي
6. **شرح موجّه لكل خيار:** لماذا الصحيح صحيح بالضبط، ولماذا الخاطئ خاطئ بالضبط (ليس شرحاً عاماً للمفهوم).
7. **تنويع الخيار الصحيح** عبر الأسئلة (لا تكون أغلبها B).
8. **اختبارات نهاية المرحلة بأسئلة جديدة** غير أسئلة الدروس.
9. **صفحات احترافية مطوّرة:** UI/UX حديث، ألوان متناسقة، تجاوب كامل، RTL.

## مرجع الملفات والمسارات
- المشروع: `C:\Users\nelt2\yamen_academy`
- DB المحلي: `academy.db` أو `data/academy.db`
- DB الإنتاج: `/app/data/academy.db` على Railway
- التطبيق: `https://yamenacademyapp.up.railway.app`
- Webhook Telegram: `/telegram-webhook` مع `secret_token=yamen-webhook-secret-2026`

## آخر تحديث
2026-06-08 00:48